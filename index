<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<meta name="theme-color" content="#1a2e1a">
<meta name="description" content="Micro Habits — 5-minute daily wellness rituals for a calmer, more intentional life.">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<title>Micro Habits — Daily Wellness Rituals</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400;1,600&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --forest: #1a2e1a;
    --moss: #2d4a2d;
    --sage: #4a7c59;
    --fern: #6aab7e;
    --mist: #a8c5a0;
    --cream: #f5f0e8;
    --parchment: #ede6d6;
    --gold: #c8a96e;
    --amber: #d4944a;
    --warm-white: #faf7f2;
    --text-dark: #1a2e1a;
    --text-mid: #3d5c3d;
    --text-light: #6b8f6b;
    --radius: 20px;
    --radius-sm: 12px;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

  html, body {
    min-height: 100dvh;
    background: var(--forest);
    font-family: 'DM Sans', sans-serif;
    color: var(--cream);
    overflow-x: hidden;
  }

  /* ── BACKGROUND TEXTURE ── */
  body::before {
    content: '';
    position: fixed; inset: 0; z-index: 0;
    background: 
      radial-gradient(ellipse 80% 60% at 20% 10%, rgba(74,124,89,0.18) 0%, transparent 60%),
      radial-gradient(ellipse 60% 80% at 80% 90%, rgba(45,74,45,0.25) 0%, transparent 60%),
      radial-gradient(ellipse 40% 40% at 60% 40%, rgba(106,171,126,0.08) 0%, transparent 50%);
    pointer-events: none;
  }

  /* Grain overlay */
  body::after {
    content: '';
    position: fixed; inset: 0; z-index: 1;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    opacity: 0.5;
  }

  /* ── APP WRAPPER ── */
  #app {
    position: relative; z-index: 2;
    max-width: 430px;
    margin: 0 auto;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    padding-bottom: 80px;
    padding-top: env(safe-area-inset-top);
    padding-left: env(safe-area-inset-left);
    padding-right: env(safe-area-inset-right);
  }

  /* ── SCREENS ── */
  .screen { display: none; flex-direction: column; flex: 1; animation: fadeIn 0.4s ease; }
  .screen.active { display: flex; }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ── HEADER ── */
  .app-header {
    padding: 24px 24px 12px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .logo-mark {
    display: flex; align-items: center; gap: 10px;
  }

  .logo-leaf {
    width: 32px; height: 32px;
    background: linear-gradient(135deg, var(--fern), var(--sage));
    border-radius: 50% 50% 50% 0;
    transform: rotate(-45deg);
    display: flex; align-items: center; justify-content: center;
    box-shadow: 0 2px 12px rgba(106,171,126,0.3);
  }

  .logo-leaf span { transform: rotate(45deg); font-size: 14px; }

  .logo-text {
    font-family: 'Playfair Display', serif;
    font-size: 18px;
    color: var(--cream);
    letter-spacing: 0.02em;
  }

  .header-streak {
    display: flex; align-items: center; gap: 6px;
    background: rgba(200,169,110,0.15);
    border: 1px solid rgba(200,169,110,0.3);
    border-radius: 20px;
    padding: 6px 12px;
    font-size: 13px;
    color: var(--gold);
    font-weight: 500;
  }

  /* ── BOTTOM NAV ── */
  .bottom-nav {
    position: fixed;
    bottom: 0; left: 50%;
    transform: translateX(-50%);
    width: 100%; max-width: 430px;
    background: rgba(26,46,26,0.92);
    backdrop-filter: blur(20px);
    border-top: 1px solid rgba(106,171,126,0.15);
    padding: 10px 0 calc(10px + env(safe-area-inset-bottom));
    display: flex;
    justify-content: space-around;
    z-index: 100;
  }

  .nav-btn {
    display: flex; flex-direction: column; align-items: center; gap: 4px;
    background: none; border: none; cursor: pointer;
    color: var(--text-light); font-size: 10px; font-family: 'DM Sans', sans-serif;
    font-weight: 500; letter-spacing: 0.05em; text-transform: uppercase;
    padding: 4px 16px; border-radius: 12px;
    transition: color 0.2s;
  }

  .nav-btn.active { color: var(--fern); }
  .nav-btn svg { width: 22px; height: 22px; }

  /* ── HOME SCREEN ── */
  .greeting-block { padding: 8px 24px 24px; }

  .greeting-time {
    font-size: 12px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.12em;
    margin-bottom: 6px;
  }

  .greeting-heading {
    font-family: 'Playfair Display', serif;
    font-size: 28px; line-height: 1.2;
    color: var(--warm-white);
  }

  .greeting-heading em { color: var(--fern); font-style: italic; }

  /* Progress ring */
  .daily-ring-wrap {
    margin: 0 24px 24px;
    background: rgba(45,74,45,0.3);
    border: 1px solid rgba(106,171,126,0.15);
    border-radius: var(--radius);
    padding: 24px;
    display: flex; align-items: center; gap: 20px;
  }

  .ring-svg { flex-shrink: 0; }
  .ring-bg { fill: none; stroke: rgba(106,171,126,0.15); stroke-width: 6; }
  .ring-fill {
    fill: none;
    stroke: var(--fern);
    stroke-width: 6;
    stroke-linecap: round;
    stroke-dasharray: 226;
    stroke-dashoffset: 226;
    transform: rotate(-90deg);
    transform-origin: center;
    transition: stroke-dashoffset 1s cubic-bezier(0.4,0,0.2,1);
  }

  .ring-label { text-anchor: middle; dominant-baseline: middle; }
  .ring-num { font-family: 'Playfair Display', serif; font-size: 22px; fill: var(--warm-white); font-weight: 600; }
  .ring-sub { font-size: 10px; fill: var(--mist); }

  .ring-info { flex: 1; }
  .ring-info-title { font-family: 'Playfair Display', serif; font-size: 17px; color: var(--warm-white); margin-bottom: 6px; }
  .ring-info-sub { font-size: 13px; color: var(--mist); line-height: 1.5; }

  /* Ritual cards */
  .section-label {
    padding: 0 24px 12px;
    font-size: 11px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.14em;
    font-weight: 500;
  }

  .rituals-list { padding: 0 24px; display: flex; flex-direction: column; gap: 12px; }

  .ritual-card {
    background: rgba(45,74,45,0.25);
    border: 1px solid rgba(106,171,126,0.12);
    border-radius: var(--radius);
    padding: 16px 18px;
    display: flex; align-items: center; gap: 16px;
    cursor: pointer;
    transition: background 0.2s, border-color 0.2s, transform 0.15s;
    position: relative; overflow: hidden;
  }

  .ritual-card::before {
    content: '';
    position: absolute; left: 0; top: 0; bottom: 0;
    width: 3px;
    background: var(--card-color, var(--fern));
    border-radius: 3px 0 0 3px;
  }

  .ritual-card:active { transform: scale(0.98); }
  .ritual-card.completed { background: rgba(74,124,89,0.2); border-color: rgba(106,171,126,0.3); }

  .ritual-icon {
    width: 44px; height: 44px; border-radius: 14px;
    background: var(--card-color-bg, rgba(106,171,126,0.15));
    display: flex; align-items: center; justify-content: center;
    font-size: 20px; flex-shrink: 0;
    transition: transform 0.3s;
  }

  .ritual-card.completed .ritual-icon { transform: scale(1.1); }

  .ritual-info { flex: 1; }
  .ritual-name { font-size: 15px; font-weight: 500; color: var(--warm-white); margin-bottom: 3px; }
  .ritual-meta { font-size: 12px; color: var(--mist); }

  .ritual-check {
    width: 28px; height: 28px; border-radius: 50%;
    border: 2px solid rgba(106,171,126,0.3);
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0; transition: all 0.3s;
  }

  .ritual-card.completed .ritual-check {
    background: var(--fern); border-color: var(--fern);
  }

  .check-svg { width: 14px; height: 14px; opacity: 0; transition: opacity 0.2s; }
  .ritual-card.completed .check-svg { opacity: 1; }

  /* Quote banner */
  .quote-banner {
    margin: 20px 24px 0;
    background: linear-gradient(135deg, rgba(200,169,110,0.12), rgba(212,148,74,0.08));
    border: 1px solid rgba(200,169,110,0.2);
    border-radius: var(--radius);
    padding: 20px;
  }

  .quote-text {
    font-family: 'Playfair Display', serif;
    font-size: 15px; font-style: italic;
    color: var(--parchment);
    line-height: 1.6; margin-bottom: 8px;
  }

  .quote-author { font-size: 11px; color: var(--gold); text-transform: uppercase; letter-spacing: 0.1em; }

  /* ── RITUAL SESSION SCREEN ── */
  .session-header {
    padding: 20px 24px;
    display: flex; align-items: center; gap: 12px;
  }

  .back-btn {
    width: 38px; height: 38px; border-radius: 12px;
    background: rgba(45,74,45,0.4); border: 1px solid rgba(106,171,126,0.15);
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; color: var(--mist); transition: background 0.2s;
  }
  .back-btn:active { background: rgba(74,124,89,0.4); }

  .session-title-block { flex: 1; }
  .session-category { font-size: 11px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.1em; }
  .session-title { font-family: 'Playfair Display', serif; font-size: 20px; color: var(--warm-white); }

  /* Timer */
  .timer-wrap {
    margin: 10px 24px 24px;
    background: rgba(45,74,45,0.3);
    border: 1px solid rgba(106,171,126,0.15);
    border-radius: 28px;
    padding: 32px 24px;
    text-align: center;
  }

  .timer-ring-wrap { position: relative; width: 160px; height: 160px; margin: 0 auto 24px; }

  .timer-svg { width: 160px; height: 160px; }
  .timer-bg { fill: none; stroke: rgba(106,171,126,0.12); stroke-width: 8; }
  .timer-track {
    fill: none;
    stroke: var(--fern);
    stroke-width: 8;
    stroke-linecap: round;
    stroke-dasharray: 440;
    stroke-dashoffset: 0;
    transform: rotate(-90deg);
    transform-origin: center;
    transition: stroke-dashoffset 1s linear, stroke 0.3s;
  }

  .timer-center {
    position: absolute; inset: 0;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
  }

  .timer-display {
    font-family: 'Playfair Display', serif;
    font-size: 38px; font-weight: 600;
    color: var(--warm-white);
    letter-spacing: -0.02em;
  }

  .timer-phase { font-size: 11px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.1em; margin-top: 2px; }

  .timer-desc {
    font-size: 14px; color: var(--mist); line-height: 1.6;
    max-width: 280px; margin: 0 auto 24px;
  }

  .timer-controls { display: flex; gap: 12px; justify-content: center; }

  .ctrl-btn {
    height: 50px; border-radius: 16px;
    border: none; cursor: pointer; font-family: 'DM Sans', sans-serif;
    font-size: 14px; font-weight: 500;
    transition: all 0.2s;
    display: flex; align-items: center; justify-content: center; gap: 8px;
  }

  .ctrl-btn.primary {
    background: var(--fern); color: var(--forest);
    padding: 0 32px; flex: 1; max-width: 180px;
    box-shadow: 0 4px 20px rgba(106,171,126,0.3);
  }
  .ctrl-btn.primary:active { transform: scale(0.97); }

  .ctrl-btn.secondary {
    background: rgba(45,74,45,0.4);
    border: 1px solid rgba(106,171,126,0.2);
    color: var(--mist);
    padding: 0 20px;
  }

  /* Steps list */
  .steps-list { padding: 0 24px; display: flex; flex-direction: column; gap: 10px; }

  .step-item {
    background: rgba(45,74,45,0.2);
    border: 1px solid rgba(106,171,126,0.1);
    border-radius: var(--radius-sm);
    padding: 14px 16px;
    display: flex; gap: 14px; align-items: flex-start;
    transition: all 0.3s;
  }

  .step-item.active-step {
    background: rgba(74,124,89,0.25);
    border-color: rgba(106,171,126,0.35);
  }

  .step-num {
    width: 26px; height: 26px; border-radius: 50%;
    background: rgba(106,171,126,0.15);
    border: 1px solid rgba(106,171,126,0.25);
    display: flex; align-items: center; justify-content: center;
    font-size: 11px; color: var(--mist); flex-shrink: 0;
    font-weight: 600;
  }

  .step-item.active-step .step-num { background: var(--fern); color: var(--forest); border-color: var(--fern); }
  .step-item.done-step .step-num { background: rgba(74,124,89,0.5); color: var(--fern); }

  .step-text { font-size: 13px; color: var(--mist); line-height: 1.55; padding-top: 4px; }
  .step-item.active-step .step-text { color: var(--cream); }

  /* ── HABITS LIBRARY SCREEN ── */
  .lib-header { padding: 16px 24px 8px; }
  .lib-title { font-family: 'Playfair Display', serif; font-size: 24px; color: var(--warm-white); margin-bottom: 4px; }
  .lib-sub { font-size: 13px; color: var(--mist); }

  .category-tabs {
    padding: 12px 24px;
    display: flex; gap: 8px; overflow-x: auto;
    scrollbar-width: none;
  }
  .category-tabs::-webkit-scrollbar { display: none; }

  .cat-tab {
    padding: 8px 16px; border-radius: 20px;
    background: rgba(45,74,45,0.3);
    border: 1px solid rgba(106,171,126,0.12);
    color: var(--mist); font-size: 12px; font-weight: 500;
    cursor: pointer; white-space: nowrap; transition: all 0.2s;
  }

  .cat-tab.active {
    background: var(--fern); color: var(--forest); border-color: var(--fern);
  }

  .habit-grid { padding: 0 24px; display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }

  .habit-tile {
    background: rgba(45,74,45,0.25);
    border: 1px solid rgba(106,171,126,0.12);
    border-radius: var(--radius);
    padding: 18px 16px;
    cursor: pointer;
    transition: all 0.2s;
    position: relative; overflow: hidden;
  }

  .habit-tile.in-routine {
    border-color: rgba(106,171,126,0.4);
    background: rgba(74,124,89,0.2);
  }

  .habit-tile:active { transform: scale(0.97); }

  .habit-tile-icon { font-size: 28px; margin-bottom: 10px; }
  .habit-tile-name { font-size: 13px; font-weight: 500; color: var(--warm-white); margin-bottom: 4px; }
  .habit-tile-dur { font-size: 11px; color: var(--mist); }

  .habit-added-badge {
    position: absolute; top: 10px; right: 10px;
    width: 20px; height: 20px; border-radius: 50%;
    background: var(--fern);
    display: none; align-items: center; justify-content: center;
  }

  .habit-tile.in-routine .habit-added-badge { display: flex; }

  /* ── PROGRESS SCREEN ── */
  .prog-header { padding: 20px 24px 16px; }
  .prog-title { font-family: 'Playfair Display', serif; font-size: 24px; color: var(--warm-white); margin-bottom: 4px; }
  .prog-sub { font-size: 13px; color: var(--mist); }

  .stats-row { padding: 0 24px; display: flex; gap: 12px; margin-bottom: 20px; }

  .stat-card {
    flex: 1;
    background: rgba(45,74,45,0.3);
    border: 1px solid rgba(106,171,126,0.15);
    border-radius: var(--radius);
    padding: 16px;
    text-align: center;
  }

  .stat-num { font-family: 'Playfair Display', serif; font-size: 28px; color: var(--warm-white); font-weight: 700; }
  .stat-label { font-size: 11px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.08em; margin-top: 4px; }

  /* Calendar heatmap */
  .heatmap-wrap {
    margin: 0 24px 20px;
    background: rgba(45,74,45,0.25);
    border: 1px solid rgba(106,171,126,0.12);
    border-radius: var(--radius);
    padding: 20px;
  }

  .heatmap-title { font-size: 13px; color: var(--mist); margin-bottom: 14px; text-transform: uppercase; letter-spacing: 0.1em; }
  .heatmap-grid { display: grid; grid-template-columns: repeat(7, 1fr); gap: 5px; }

  .heatmap-day {
    aspect-ratio: 1;
    border-radius: 5px;
    background: rgba(106,171,126,0.08);
    transition: background 0.3s;
  }
  .heatmap-day.done { background: rgba(106,171,126,0.5); }
  .heatmap-day.today { background: var(--fern); box-shadow: 0 0 8px rgba(106,171,126,0.5); }
  .heatmap-day.perfect { background: var(--gold); }

  /* Achievements */
  .achieve-section { padding: 0 24px; }
  .achieve-title { font-size: 11px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 12px; }
  .achieve-row { display: flex; gap: 10px; flex-wrap: wrap; }

  .badge {
    display: flex; flex-direction: column; align-items: center; gap: 6px;
    background: rgba(45,74,45,0.3);
    border: 1px solid rgba(106,171,126,0.15);
    border-radius: 16px;
    padding: 14px 12px;
    width: calc(33.33% - 7px);
    text-align: center;
    transition: all 0.3s;
  }

  .badge.earned { border-color: rgba(200,169,110,0.4); background: rgba(200,169,110,0.1); }
  .badge.locked { opacity: 0.45; }

  .badge-icon { font-size: 24px; }
  .badge-name { font-size: 10px; color: var(--mist); font-weight: 500; }
  .badge.earned .badge-name { color: var(--gold); }

  /* ── SETTINGS / PROFILE ── */
  .profile-wrap { padding: 20px 24px; }
  .profile-top { display: flex; align-items: center; gap: 16px; margin-bottom: 24px; }
  
  .avatar {
    width: 60px; height: 60px; border-radius: 20px;
    background: linear-gradient(135deg, var(--sage), var(--fern));
    display: flex; align-items: center; justify-content: center;
    font-size: 24px;
  }

  .profile-name { font-family: 'Playfair Display', serif; font-size: 20px; color: var(--warm-white); }
  .profile-since { font-size: 12px; color: var(--mist); }

  .settings-section { margin-bottom: 20px; }
  .settings-label { font-size: 11px; color: var(--mist); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 10px; }

  .settings-row {
    background: rgba(45,74,45,0.25);
    border: 1px solid rgba(106,171,126,0.1);
    border-radius: var(--radius-sm);
    display: flex; flex-direction: column;
    overflow: hidden;
  }

  .settings-item {
    display: flex; align-items: center; justify-content: space-between;
    padding: 14px 16px;
    border-bottom: 1px solid rgba(106,171,126,0.08);
    cursor: pointer; transition: background 0.15s;
  }

  .settings-item:last-child { border-bottom: none; }
  .settings-item:active { background: rgba(74,124,89,0.2); }

  .settings-item-left { display: flex; align-items: center; gap: 12px; }
  .settings-item-icon { font-size: 18px; }
  .settings-item-text { font-size: 14px; color: var(--cream); }
  .settings-item-value { font-size: 13px; color: var(--mist); }

  /* Toggle */
  .toggle {
    width: 44px; height: 26px; border-radius: 13px;
    background: rgba(106,171,126,0.2); border: none; cursor: pointer;
    position: relative; transition: background 0.3s; flex-shrink: 0;
  }
  .toggle::after {
    content: ''; position: absolute;
    width: 20px; height: 20px; border-radius: 50%;
    background: var(--mist); top: 3px; left: 3px;
    transition: all 0.3s;
  }
  .toggle.on { background: var(--fern); }
  .toggle.on::after { left: 21px; background: var(--forest); }

  /* ── COMPLETION OVERLAY ── */
  .completion-overlay {
    display: none;
    position: fixed; inset: 0; z-index: 200;
    background: rgba(26,46,26,0.92);
    backdrop-filter: blur(10px);
    align-items: center; justify-content: center;
    flex-direction: column;
    text-align: center;
    padding: 40px;
    animation: fadeIn 0.4s ease;
  }

  .completion-overlay.show { display: flex; }

  .completion-emoji { font-size: 64px; margin-bottom: 20px; animation: bounce 0.6s ease 0.2s both; }
  .completion-title { font-family: 'Playfair Display', serif; font-size: 28px; color: var(--warm-white); margin-bottom: 10px; }
  .completion-sub { font-size: 15px; color: var(--mist); line-height: 1.6; margin-bottom: 32px; }

  .completion-streak {
    display: flex; align-items: center; gap: 8px;
    background: rgba(200,169,110,0.15);
    border: 1px solid rgba(200,169,110,0.3);
    border-radius: 20px; padding: 10px 20px;
    color: var(--gold); font-size: 15px; font-weight: 500;
    margin-bottom: 24px;
  }

  .completion-btn {
    background: var(--fern); color: var(--forest);
    border: none; border-radius: 16px;
    padding: 14px 40px; font-size: 15px; font-weight: 600;
    cursor: pointer; font-family: 'DM Sans', sans-serif;
    box-shadow: 0 4px 24px rgba(106,171,126,0.4);
    transition: transform 0.2s;
  }
  .completion-btn:active { transform: scale(0.97); }

  @keyframes bounce {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-16px) scale(1.1); }
  }

  /* ── CONFETTI ── */
  .confetti-piece {
    position: fixed; width: 8px; height: 8px;
    border-radius: 2px;
    animation: confettiFall linear forwards;
    pointer-events: none; z-index: 300;
  }

  @keyframes confettiFall {
    0% { transform: translateY(-20px) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
  }

  /* Scrollable content */
  .scroll-content { flex: 1; overflow-y: auto; padding-bottom: 20px; scrollbar-width: none; }
  .scroll-content::-webkit-scrollbar { display: none; }

  /* Breathing animation for active ritual */
  .breathe-anim {
    width: 80px; height: 80px; border-radius: 50%;
    background: radial-gradient(circle, rgba(106,171,126,0.4), rgba(74,124,89,0.1));
    margin: 0 auto 16px;
    animation: breathe 4s ease-in-out infinite;
  }

  @keyframes breathe {
    0%, 100% { transform: scale(1); opacity: 0.7; }
    50% { transform: scale(1.3); opacity: 1; }
  }

  /* Pulse on complete */
  @keyframes pulseGreen {
    0% { box-shadow: 0 0 0 0 rgba(106,171,126,0.5); }
    70% { box-shadow: 0 0 0 16px rgba(106,171,126,0); }
    100% { box-shadow: 0 0 0 0 rgba(106,171,126,0); }
  }

  .ritual-card.just-completed { animation: pulseGreen 0.6s ease; }

  /* Premium banner */
  .premium-banner {
    margin: 20px 24px 0;
    background: linear-gradient(135deg, rgba(200,169,110,0.18), rgba(212,148,74,0.12));
    border: 1px solid rgba(200,169,110,0.25);
    border-radius: var(--radius);
    padding: 18px;
    display: flex; align-items: center; justify-content: space-between; gap: 12px;
  }

  .premium-text { flex: 1; }
  .premium-title { font-size: 14px; font-weight: 600; color: var(--gold); margin-bottom: 3px; }
  .premium-sub { font-size: 12px; color: var(--mist); }

  .premium-cta {
    background: linear-gradient(135deg, var(--gold), var(--amber));
    color: var(--forest); border: none; border-radius: 12px;
    padding: 8px 16px; font-size: 12px; font-weight: 700;
    cursor: pointer; white-space: nowrap;
    font-family: 'DM Sans', sans-serif;
  }

  /* Time of day pill */
  .tod-pills {
    display: flex; gap: 8px; padding: 0 24px 16px;
  }

  .tod-pill {
    padding: 6px 14px; border-radius: 20px;
    border: 1px solid rgba(106,171,126,0.15);
    background: rgba(45,74,45,0.2);
    color: var(--mist); font-size: 12px; cursor: pointer;
    transition: all 0.2s;
  }

  .tod-pill.active { background: var(--moss); border-color: var(--sage); color: var(--mist); }

  /* Active dots */
  .dot { width: 5px; height: 5px; border-radius: 50%; background: var(--fern); display: inline-block; }
</style>
</head>
<body>

<div id="app">

  <!-- ══ HOME SCREEN ══ -->
  <div id="screen-home" class="screen active">
    <div class="app-header">
      <div class="logo-mark">
        <div class="logo-leaf"><span>🌿</span></div>
        <span class="logo-text">Micro Habits</span>
      </div>
      <div class="header-streak" id="streak-display">🔥 <span id="streak-num">0</span> day streak</div>
    </div>

    <div class="scroll-content">
      <div class="greeting-block">
        <div class="greeting-time" id="greeting-time">Good morning</div>
        <div class="greeting-heading">
          Build <em>small</em>,<br>grow consistently.
        </div>
      </div>

      <div class="tod-pills">
        <div class="tod-pill active" onclick="filterByTime('all')">All</div>
        <div class="tod-pill" onclick="filterByTime('morning')">🌅 Morning</div>
        <div class="tod-pill" onclick="filterByTime('afternoon')">☀️ Afternoon</div>
        <div class="tod-pill" onclick="filterByTime('evening')">🌙 Evening</div>
      </div>

      <!-- Daily Progress Ring -->
      <div class="daily-ring-wrap">
        <svg class="ring-svg" width="80" height="80" viewBox="0 0 80 80">
          <circle class="ring-bg" cx="40" cy="40" r="36"/>
          <circle class="ring-fill" id="progress-ring" cx="40" cy="40" r="36"/>
          <text x="40" y="37" class="ring-label">
            <tspan class="ring-num" id="ring-done">0</tspan>
          </text>
          <text x="40" y="51" class="ring-label">
            <tspan class="ring-sub" id="ring-total">of 5</tspan>
          </text>
        </svg>
        <div class="ring-info">
          <div class="ring-info-title" id="ring-title">Start your rituals</div>
          <div class="ring-info-sub" id="ring-subtitle">Complete 5 micro habits today — just 5 minutes total.</div>
        </div>
      </div>

      <div class="section-label">TODAY'S RITUALS</div>

      <div class="rituals-list" id="rituals-list">
        <!-- Populated by JS -->
      </div>

      <div class="quote-banner">
        <div class="quote-text" id="daily-quote">"We are what we repeatedly do. Excellence, then, is not an act, but a habit."</div>
        <div class="quote-author" id="quote-author">— Aristotle</div>
      </div>

      <div class="premium-banner">
        <div class="premium-text">
          <div class="premium-title">✨ Unlock Premium</div>
          <div class="premium-sub">50+ rituals, custom routines, sleep tracking & more</div>
        </div>
        <button class="premium-cta">$6.99/mo</button>
      </div>

    </div>
  </div>

  <!-- ══ SESSION SCREEN ══ -->
  <div id="screen-session" class="screen">
    <div class="session-header">
      <div class="back-btn" onclick="goBack()">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M19 12H5M12 5l-7 7 7 7"/>
        </svg>
      </div>
      <div class="session-title-block">
        <div class="session-category" id="session-cat">MORNING RITUAL</div>
        <div class="session-title" id="session-title">Deep Breathing</div>
      </div>
    </div>

    <div class="scroll-content">

      <div class="timer-wrap">
        <div id="breathe-wrap" style="display:none"><div class="breathe-anim"></div></div>
        <div class="timer-ring-wrap">
          <svg class="timer-svg" viewBox="0 0 160 160">
            <circle class="timer-bg" cx="80" cy="80" r="70"/>
            <circle class="timer-track" id="timer-track" cx="80" cy="80" r="70"/>
          </svg>
          <div class="timer-center">
            <div class="timer-display" id="timer-display">5:00</div>
            <div class="timer-phase" id="timer-phase">Ready</div>
          </div>
        </div>

        <div class="timer-desc" id="session-desc">
          Take a moment to settle in. Find a comfortable position and prepare to begin.
        </div>

        <div class="timer-controls">
          <button class="ctrl-btn secondary" onclick="resetTimer()">↺ Reset</button>
          <button class="ctrl-btn primary" id="timer-btn" onclick="toggleTimer()">
            ▶ Begin
          </button>
        </div>
      </div>

      <div class="section-label">RITUAL STEPS</div>
      <div class="steps-list" id="steps-list">
        <!-- Populated by JS -->
      </div>

    </div>
  </div>

  <!-- ══ LIBRARY SCREEN ══ -->
  <div id="screen-library" class="screen">
    <div class="lib-header">
      <div class="lib-title">Habit Library</div>
      <div class="lib-sub">Tap to add habits to your daily routine</div>
    </div>

    <div class="category-tabs" id="category-tabs">
      <div class="cat-tab active" onclick="filterCategory('all')">All</div>
      <div class="cat-tab" onclick="filterCategory('breath')">🌬️ Breath</div>
      <div class="cat-tab" onclick="filterCategory('movement')">🏃 Movement</div>
      <div class="cat-tab" onclick="filterCategory('mind')">🧘 Mindfulness</div>
      <div class="cat-tab" onclick="filterCategory('gratitude')">🙏 Gratitude</div>
      <div class="cat-tab" onclick="filterCategory('energy')">⚡ Energy</div>
    </div>

    <div class="scroll-content" style="padding:0 24px 20px">
      <div class="habit-grid" id="habit-grid">
        <!-- Populated by JS -->
      </div>
    </div>
  </div>

  <!-- ══ PROGRESS SCREEN ══ -->
  <div id="screen-progress" class="screen">
    <div class="prog-header">
      <div class="prog-title">Your Progress</div>
      <div class="prog-sub">Every day counts. Keep building.</div>
    </div>

    <div class="scroll-content">

      <div class="stats-row">
        <div class="stat-card">
          <div class="stat-num" id="stat-streak">0</div>
          <div class="stat-label">Day Streak</div>
        </div>
        <div class="stat-card">
          <div class="stat-num" id="stat-total">0</div>
          <div class="stat-label">Total Done</div>
        </div>
        <div class="stat-card">
          <div class="stat-num" id="stat-best">0</div>
          <div class="stat-label">Best Streak</div>
        </div>
      </div>

      <div class="heatmap-wrap">
        <div class="heatmap-title">Last 28 Days</div>
        <div class="heatmap-grid" id="heatmap-grid"></div>
      </div>

      <div class="achieve-section">
        <div class="achieve-title">Achievements</div>
        <div class="achieve-row" id="badges-row"></div>
      </div>

    </div>
  </div>

  <!-- ══ PROFILE SCREEN ══ -->
  <div id="screen-profile" class="screen">
    <div class="scroll-content">
      <div class="profile-wrap">
        <div class="profile-top">
          <div class="avatar">🌱</div>
          <div>
            <div class="profile-name">Wellness Seeker</div>
            <div class="profile-since">Member since today</div>
          </div>
        </div>

        <div class="settings-section">
          <div class="settings-label">Reminders</div>
          <div class="settings-row">
            <div class="settings-item">
              <div class="settings-item-left">
                <div class="settings-item-icon">🌅</div>
                <div class="settings-item-text">Morning Reminder</div>
              </div>
              <div style="display:flex;align-items:center;gap:10px">
                <span class="settings-item-value">7:00 AM</span>
                <button class="toggle on" onclick="this.classList.toggle('on')"></button>
              </div>
            </div>
            <div class="settings-item">
              <div class="settings-item-left">
                <div class="settings-item-icon">🌙</div>
                <div class="settings-item-text">Evening Reminder</div>
              </div>
              <div style="display:flex;align-items:center;gap:10px">
                <span class="settings-item-value">8:00 PM</span>
                <button class="toggle" onclick="this.classList.toggle('on')"></button>
              </div>
            </div>
          </div>
        </div>

        <div class="settings-section">
          <div class="settings-label">Preferences</div>
          <div class="settings-row">
            <div class="settings-item">
              <div class="settings-item-left">
                <div class="settings-item-icon">🎯</div>
                <div class="settings-item-text">Daily Goal</div>
              </div>
              <span class="settings-item-value" id="goal-display">5 habits</span>
            </div>
            <div class="settings-item">
              <div class="settings-item-left">
                <div class="settings-item-icon">🔔</div>
                <div class="settings-item-text">Sound Effects</div>
              </div>
              <button class="toggle on" onclick="this.classList.toggle('on')"></button>
            </div>
            <div class="settings-item">
              <div class="settings-item-left">
                <div class="settings-item-icon">📳</div>
                <div class="settings-item-text">Haptic Feedback</div>
              </div>
              <button class="toggle on" onclick="this.classList.toggle('on')"></button>
            </div>
          </div>
        </div>

        <div class="settings-section">
          <div class="settings-label">About</div>
          <div class="settings-row">
            <div class="settings-item">
              <div class="settings-item-left">
                <div class="settings-item-icon">🌿</div>
                <div class="settings-item-text">Micro Habits</div>
              </div>
              <span class="settings-item-value">v1.0</span>
            </div>
            <div class="settings-item" onclick="showToast('Built with ❤️ by Toastid Tech LLC')">
              <div class="settings-item-left">
                <div class="settings-item-icon">🍞</div>
                <div class="settings-item-text">Toastid Tech LLC</div>
              </div>
              <span class="settings-item-value">toastidtech.com</span>
            </div>
          </div>
        </div>

      </div>
    </div>
  </div>

  <!-- ══ BOTTOM NAV ══ -->
  <nav class="bottom-nav">
    <button class="nav-btn active" id="nav-home" onclick="navigate('home')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
        <polyline points="9,22 9,12 15,12 15,22"/>
      </svg>
      Home
    </button>
    <button class="nav-btn" id="nav-library" onclick="navigate('library')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/>
        <rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>
      </svg>
      Library
    </button>
    <button class="nav-btn" id="nav-progress" onclick="navigate('progress')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="22,12 18,12 15,21 9,3 6,12 2,12"/>
      </svg>
      Progress
    </button>
    <button class="nav-btn" id="nav-profile" onclick="navigate('profile')">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
        <circle cx="12" cy="7" r="4"/>
      </svg>
      Profile
    </button>
  </nav>

</div>

<!-- Completion overlay -->
<div class="completion-overlay" id="completion-overlay">
  <div class="completion-emoji" id="comp-emoji">🎉</div>
  <div class="completion-title" id="comp-title">Ritual Complete!</div>
  <div class="completion-sub" id="comp-sub">You showed up for yourself today.<br>That's everything.</div>
  <div class="completion-streak" id="comp-streak">🔥 1 day streak</div>
  <button class="completion-btn" onclick="closeCompletion()">Continue →</button>
</div>

<!-- Toast -->
<div id="toast" style="
  position:fixed; bottom:100px; left:50%; transform:translateX(-50%) translateY(20px);
  background:rgba(45,74,45,0.95); border:1px solid rgba(106,171,126,0.3);
  color:var(--cream); padding:10px 20px; border-radius:20px;
  font-size:13px; z-index:500; opacity:0;
  transition:all 0.3s; pointer-events:none; white-space:nowrap;
"></div>

<script>
// ══════════════════════════════════════
// DATA
// ══════════════════════════════════════

const HABITS_DB = [
  {
    id: 'deep-breath', name: 'Deep Breathing', icon: '🌬️', category: 'breath',
    duration: 60, time: 'morning',
    color: '#6aab7e', colorBg: 'rgba(106,171,126,0.15)',
    desc: 'Activate your parasympathetic nervous system with 4-7-8 breathing.',
    steps: [
      'Find a comfortable seated position. Close your eyes.',
      'Inhale slowly through your nose for 4 counts.',
      'Hold your breath gently for 7 counts.',
      'Exhale completely through your mouth for 8 counts.',
      'Repeat this cycle 4 times, then breathe naturally.'
    ],
    breathe: true
  },
  {
    id: 'gratitude', name: 'Gratitude Scan', icon: '🙏', category: 'gratitude',
    duration: 60, time: 'morning',
    color: '#c8a96e', colorBg: 'rgba(200,169,110,0.15)',
    desc: 'Name three specific things you are grateful for right now.',
    steps: [
      'Take one slow, deep breath to center yourself.',
      'Think of something small you\'re grateful for today.',
      'Name one person who has helped or supported you.',
      'Identify one thing about your body or health you appreciate.',
      'Hold these thoughts for 20 seconds, then smile.'
    ]
  },
  {
    id: 'body-scan', name: 'Body Scan', icon: '🧘', category: 'mind',
    duration: 90, time: 'morning',
    color: '#9b8abf', colorBg: 'rgba(155,138,191,0.15)',
    desc: 'A brief mindful scan from head to toe to check in with yourself.',
    steps: [
      'Close your eyes and take 3 natural breaths.',
      'Notice any tension in your head, face, and neck.',
      'Scan down through shoulders, chest, and belly.',
      'Check in with your hips, legs, and feet.',
      'Take one final breath and gently open your eyes.'
    ]
  },
  {
    id: 'stretch', name: 'Morning Stretch', icon: '🤸', category: 'movement',
    duration: 120, time: 'morning',
    color: '#d4944a', colorBg: 'rgba(212,148,74,0.15)',
    desc: 'Wake up your body with gentle movement after sleep.',
    steps: [
      'Stand tall and reach both arms overhead — 10 seconds.',
      'Roll your shoulders back 5 times, then forward 5 times.',
      'Gentle neck rolls — 3 each direction, slow and smooth.',
      'Forward fold — let your arms hang heavy for 15 seconds.',
      'Rise up slowly and shake out your hands and wrists.'
    ]
  },
  {
    id: 'affirmation', name: 'Power Affirmation', icon: '✨', category: 'mind',
    duration: 60, time: 'morning',
    color: '#6aab7e', colorBg: 'rgba(106,171,126,0.15)',
    desc: 'Speak your intentions into existence with a focused affirmation.',
    steps: [
      'Stand or sit with your spine straight.',
      'Take one deep breath, then look straight ahead.',
      'Say aloud: "I am capable, focused, and ready for today."',
      'Say aloud: "I choose calm over chaos, growth over comfort."',
      'Take a final breath and carry this intention into your day.'
    ]
  },
  {
    id: 'hydrate', name: 'Mindful Hydration', icon: '💧', category: 'energy',
    duration: 60, time: 'morning',
    color: '#4a9ebe', colorBg: 'rgba(74,158,190,0.15)',
    desc: 'Intentionally rehydrate your body after sleep with full presence.',
    steps: [
      'Pour a full glass of water. Hold it in both hands.',
      'Notice the temperature and weight of the glass.',
      'Take your first sip slowly and mindfully.',
      'Drink the full glass in deliberate, unhurried sips.',
      'Set an intention for the day as you finish.'
    ]
  },
  {
    id: 'box-breath', name: 'Box Breathing', icon: '⬜', category: 'breath',
    duration: 90, time: 'afternoon',
    color: '#6aab7e', colorBg: 'rgba(106,171,126,0.15)',
    desc: 'The same technique used by Navy SEALs to reset under pressure.',
    steps: [
      'Sit upright. Exhale fully to empty your lungs.',
      'Inhale for exactly 4 counts, filling from the belly up.',
      'Hold at the top for 4 counts — stay relaxed.',
      'Exhale for 4 counts, releasing slowly and fully.',
      'Hold empty for 4 counts. Repeat 4-6 cycles.'
    ],
    breathe: true
  },
  {
    id: 'desk-reset', name: 'Desk Reset', icon: '💼', category: 'movement',
    duration: 60, time: 'afternoon',
    color: '#d4944a', colorBg: 'rgba(212,148,74,0.15)',
    desc: 'Break the sitting spell and re-energize your afternoon.',
    steps: [
      'Stand up from your desk and shake out your arms.',
      'Do 10 slow calf raises, holding the top for 1 second each.',
      'Roll your wrists 5x each direction.',
      'Do 5 chest-opening arm circles — wide and deliberate.',
      'Stand tall for 10 seconds before sitting back down.'
    ]
  },
  {
    id: 'focus-reset', name: 'Focus Reset', icon: '🎯', category: 'mind',
    duration: 60, time: 'afternoon',
    color: '#9b8abf', colorBg: 'rgba(155,138,191,0.15)',
    desc: 'Clear mental fog and re-engage with your most important task.',
    steps: [
      'Close all tabs and apps. Set your phone face down.',
      'Write one sentence: "My most important task right now is..."',
      'Take 5 slow breaths — one for each finger.',
      'Re-read what you wrote. Commit to just 10 minutes on it.',
      'Begin. Let momentum carry you further.'
    ]
  },
  {
    id: 'wind-down', name: 'Evening Wind-Down', icon: '🌙', category: 'mind',
    duration: 120, time: 'evening',
    color: '#7b9bbf', colorBg: 'rgba(123,155,191,0.15)',
    desc: 'Transition your nervous system from "on" to "off" mode.',
    steps: [
      'Dim the lights. Put your phone in another room if possible.',
      'Take 5 deep, slow breaths — exhale twice as long as inhale.',
      'Name one thing that went well today, however small.',
      'Release any unfinished thoughts — write them down if needed.',
      'Set your intention for tomorrow in one clear sentence.'
    ]
  },
  {
    id: 'journal', name: 'Micro Journal', icon: '📓', category: 'gratitude',
    duration: 90, time: 'evening',
    color: '#c8a96e', colorBg: 'rgba(200,169,110,0.15)',
    desc: '3 questions. 3 minutes. A clearer mind before sleep.',
    steps: [
      'Open your journal or notes app.',
      'Write: "Today I am proud of..." (1-2 sentences)',
      'Write: "Today I learned..." (1 sentence)',
      'Write: "Tomorrow I will focus on..." (1 sentence)',
      'Close the journal. Your mind can rest now.'
    ]
  },
  {
    id: 'cold-splash', name: 'Cold Splash', icon: '🚿', category: 'energy',
    duration: 60, time: 'morning',
    color: '#4a9ebe', colorBg: 'rgba(74,158,190,0.15)',
    desc: 'A 20-second cold water face splash to spike alertness and mood.',
    steps: [
      'Lean over your sink. Take one full breath.',
      'Turn the water to cold. Let it run for 5 seconds.',
      'Splash cold water on your face 5 times.',
      'Cup cold water in your hands and hold your face in it for 5 seconds.',
      'Pat dry. Notice the immediate shift in alertness.'
    ]
  }
];

const QUOTES = [
  { text: "We are what we repeatedly do. Excellence, then, is not an act, but a habit.", author: "Aristotle" },
  { text: "Small daily improvements are the key to staggering long-term results.", author: "Robin Sharma" },
  { text: "You don't rise to the level of your goals. You fall to the level of your systems.", author: "James Clear" },
  { text: "The secret of getting ahead is getting started.", author: "Mark Twain" },
  { text: "A year from now you'll wish you had started today.", author: "Karen Lamb" },
  { text: "Take care of your body. It's the only place you have to live.", author: "Jim Rohn" },
  { text: "The groundwork of all happiness is health.", author: "Leigh Hunt" }
];

const BADGES = [
  { id: 'first', name: 'First Step', icon: '🌱', req: 1, type: 'total' },
  { id: 'week', name: '7-Day Run', icon: '🔥', req: 7, type: 'streak' },
  { id: 'ten', name: 'Ten Done', icon: '💪', req: 10, type: 'total' },
  { id: 'month', name: '30-Day Pro', icon: '🏆', req: 30, type: 'streak' },
  { id: 'fifty', name: 'Half Century', icon: '⭐', req: 50, type: 'total' },
  { id: 'zen', name: 'Zen Master', icon: '🧘', req: 100, type: 'total' }
];

// ══════════════════════════════════════
// STATE
// ══════════════════════════════════════

let state = loadState();
let timerInterval = null;
let timerRunning = false;
let timerSeconds = 0;
let timerMax = 300;
let currentHabitId = null;
let currentFilter = 'all';
let currentCatFilter = 'all';
let activeScreen = 'home';

function loadState() {
  try {
    const saved = localStorage.getItem('microhabits_state');
    if (saved) return JSON.parse(saved);
  } catch(e) {}
  return {
    streak: 0,
    bestStreak: 0,
    totalDone: 0,
    completedToday: [],
    lastCompletedDate: null,
    history: [], // array of {date, count}
    routine: ['deep-breath', 'gratitude', 'stretch', 'hydrate', 'box-breath']
  };
}

function saveState() {
  try { localStorage.setItem('microhabits_state', JSON.stringify(state)); } catch(e) {}
}

function todayStr() {
  return new Date().toISOString().split('T')[0];
}

function checkNewDay() {
  const today = todayStr();
  if (state.lastCompletedDate !== today) {
    // Check if streak should continue
    const yesterday = new Date();
    yesterday.setDate(yesterday.getDate() - 1);
    const ystStr = yesterday.toISOString().split('T')[0];
    if (state.lastCompletedDate !== ystStr && state.completedToday.length > 0) {
      state.streak = 0; // broken
    }
    state.completedToday = [];
  }
}

// ══════════════════════════════════════
// NAVIGATION
// ══════════════════════════════════════

function navigate(screen) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
  document.getElementById('screen-' + screen).classList.add('active');
  document.getElementById('nav-' + screen)?.classList.add('active');
  activeScreen = screen;

  if (screen === 'home') renderHome();
  if (screen === 'progress') renderProgress();
  if (screen === 'library') renderLibrary();
}

function goBack() {
  clearTimer();
  navigate('home');
}

// ══════════════════════════════════════
// HOME SCREEN
// ══════════════════════════════════════

function renderHome() {
  checkNewDay();
  setGreeting();
  setQuote();
  renderRituals();
  updateRing();
  updateStreakDisplay();
}

function setGreeting() {
  const h = new Date().getHours();
  let g = h < 12 ? 'Good morning' : h < 17 ? 'Good afternoon' : 'Good evening';
  document.getElementById('greeting-time').textContent = g;
}

function setQuote() {
  const q = QUOTES[new Date().getDay() % QUOTES.length];
  document.getElementById('daily-quote').textContent = '"' + q.text + '"';
  document.getElementById('quote-author').textContent = '— ' + q.author;
}

function renderRituals() {
  const list = document.getElementById('rituals-list');
  const routineHabits = state.routine.map(id => HABITS_DB.find(h => h.id === id)).filter(Boolean);
  const filtered = currentFilter === 'all' ? routineHabits : routineHabits.filter(h => h.time === currentFilter);

  list.innerHTML = filtered.map(habit => {
    const done = state.completedToday.includes(habit.id);
    return `
      <div class="ritual-card ${done ? 'completed' : ''}" 
           style="--card-color:${habit.color};--card-color-bg:${habit.colorBg}"
           onclick="startRitual('${habit.id}')">
        <div class="ritual-icon">${habit.icon}</div>
        <div class="ritual-info">
          <div class="ritual-name">${habit.name}</div>
          <div class="ritual-meta">${habit.time.charAt(0).toUpperCase() + habit.time.slice(1)} · ${habit.duration}s</div>
        </div>
        <div class="ritual-check">
          <svg class="check-svg" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20,6 9,17 4,12"/>
          </svg>
        </div>
      </div>
    `;
  }).join('');
}

function filterByTime(time) {
  currentFilter = time;
  document.querySelectorAll('.tod-pill').forEach(p => p.classList.remove('active'));
  event.target.classList.add('active');
  renderRituals();
}

function updateRing() {
  const total = state.routine.length;
  const done = state.completedToday.length;
  const circ = 226;
  const offset = circ - (done / total) * circ;
  document.getElementById('progress-ring').style.strokeDashoffset = offset;
  document.getElementById('ring-done').textContent = done;
  document.getElementById('ring-total').textContent = 'of ' + total;

  if (done === 0) {
    document.getElementById('ring-title').textContent = 'Start your rituals';
    document.getElementById('ring-subtitle').textContent = 'Just 5 minutes. You\'ve got this.';
  } else if (done < total) {
    document.getElementById('ring-title').textContent = `${done} of ${total} complete`;
    document.getElementById('ring-subtitle').textContent = `${total - done} ritual${total - done > 1 ? 's' : ''} remaining today.`;
  } else {
    document.getElementById('ring-title').textContent = '🎉 All done today!';
    document.getElementById('ring-subtitle').textContent = 'You\'ve completed all your rituals. Come back tomorrow!';
    document.getElementById('progress-ring').style.stroke = '#c8a96e';
  }
}

function updateStreakDisplay() {
  document.getElementById('streak-num').textContent = state.streak;
  document.getElementById('stat-streak').textContent = state.streak;
  document.getElementById('stat-total').textContent = state.totalDone;
  document.getElementById('stat-best').textContent = state.bestStreak;
}

// ══════════════════════════════════════
// SESSION / TIMER
// ══════════════════════════════════════

function startRitual(id) {
  const habit = HABITS_DB.find(h => h.id === id);
  if (!habit) return;
  currentHabitId = id;

  document.getElementById('session-cat').textContent = habit.time.toUpperCase() + ' RITUAL';
  document.getElementById('session-title').textContent = habit.name;
  document.getElementById('session-desc').textContent = habit.desc;

  // Breathing animation
  document.getElementById('breathe-wrap').style.display = habit.breathe ? 'block' : 'none';

  timerMax = habit.duration;
  timerSeconds = habit.duration;
  timerRunning = false;

  document.getElementById('timer-btn').textContent = '▶ Begin';
  document.getElementById('timer-phase').textContent = 'Ready';
  renderTimerDisplay();
  resetTimerRing();

  // Render steps
  const stepsList = document.getElementById('steps-list');
  stepsList.innerHTML = habit.steps.map((step, i) => `
    <div class="step-item ${i === 0 ? 'active-step' : ''}" id="step-${i}">
      <div class="step-num">${i + 1}</div>
      <div class="step-text">${step}</div>
    </div>
  `).join('');

  navigate('session');
  document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
}

function toggleTimer() {
  if (timerRunning) {
    pauseTimer();
  } else {
    startTimer();
  }
}

function startTimer() {
  timerRunning = true;
  document.getElementById('timer-btn').textContent = '⏸ Pause';
  document.getElementById('timer-phase').textContent = 'In Progress';

  const totalSteps = HABITS_DB.find(h => h.id === currentHabitId)?.steps.length || 5;

  timerInterval = setInterval(() => {
    timerSeconds--;

    // Update step highlight
    const elapsed = timerMax - timerSeconds;
    const stepDur = timerMax / totalSteps;
    const activeStep = Math.min(Math.floor(elapsed / stepDur), totalSteps - 1);
    document.querySelectorAll('.step-item').forEach((el, i) => {
      el.classList.remove('active-step', 'done-step');
      if (i < activeStep) el.classList.add('done-step');
      if (i === activeStep) el.classList.add('active-step');
    });

    renderTimerDisplay();
    updateTimerRing();

    if (timerSeconds <= 0) {
      clearInterval(timerInterval);
      timerRunning = false;
      completeRitual();
    }
  }, 1000);
}

function pauseTimer() {
  clearInterval(timerInterval);
  timerRunning = false;
  document.getElementById('timer-btn').textContent = '▶ Resume';
  document.getElementById('timer-phase').textContent = 'Paused';
}

function resetTimer() {
  clearTimer();
  timerSeconds = timerMax;
  timerRunning = false;
  document.getElementById('timer-btn').textContent = '▶ Begin';
  document.getElementById('timer-phase').textContent = 'Ready';
  renderTimerDisplay();
  resetTimerRing();
  document.querySelectorAll('.step-item').forEach((el, i) => {
    el.classList.remove('active-step', 'done-step');
    if (i === 0) el.classList.add('active-step');
  });
}

function clearTimer() {
  if (timerInterval) { clearInterval(timerInterval); timerInterval = null; }
}

function renderTimerDisplay() {
  const m = Math.floor(timerSeconds / 60);
  const s = timerSeconds % 60;
  document.getElementById('timer-display').textContent = `${m}:${s.toString().padStart(2, '0')}`;
}

function updateTimerRing() {
  const circ = 440;
  const pct = timerSeconds / timerMax;
  document.getElementById('timer-track').style.strokeDashoffset = circ * (1 - pct);
  if (timerSeconds <= 10) {
    document.getElementById('timer-track').style.stroke = '#d4944a';
  }
}

function resetTimerRing() {
  document.getElementById('timer-track').style.strokeDashoffset = '0';
  document.getElementById('timer-track').style.stroke = 'var(--fern)';
}

// ══════════════════════════════════════
// COMPLETION
// ══════════════════════════════════════

function completeRitual() {
  const id = currentHabitId;
  if (!id) return;

  // Mark complete
  if (!state.completedToday.includes(id)) {
    state.completedToday.push(id);
    state.totalDone++;

    const today = todayStr();
    if (state.lastCompletedDate !== today) {
      state.streak++;
      if (state.streak > state.bestStreak) state.bestStreak = state.streak;
      state.lastCompletedDate = today;
      // Record history
      state.history.push({ date: today, count: state.completedToday.length });
    } else {
      // Update today's history
      const idx = state.history.findIndex(h => h.date === today);
      if (idx >= 0) state.history[idx].count = state.completedToday.length;
      else state.history.push({ date: today, count: state.completedToday.length });
    }
    saveState();
  }

  const habit = HABITS_DB.find(h => h.id === id);
  const allDone = state.completedToday.length >= state.routine.length;

  document.getElementById('comp-emoji').textContent = allDone ? '🏆' : '🎉';
  document.getElementById('comp-title').textContent = allDone ? 'All Rituals Complete!' : `${habit.name} Done!`;
  document.getElementById('comp-sub').textContent = allDone
    ? 'You completed every ritual today.\nThis is how transformation happens.'
    : `You showed up for yourself.\nThat's what matters most.`;
  document.getElementById('comp-streak').textContent = `🔥 ${state.streak} day streak`;
  document.getElementById('completion-overlay').classList.add('show');

  spawnConfetti(allDone ? 60 : 30);
}

function closeCompletion() {
  document.getElementById('completion-overlay').classList.remove('show');
  navigate('home');
}

function spawnConfetti(count) {
  const colors = ['#6aab7e', '#c8a96e', '#d4944a', '#a8c5a0', '#faf7f2'];
  for (let i = 0; i < count; i++) {
    setTimeout(() => {
      const el = document.createElement('div');
      el.className = 'confetti-piece';
      el.style.cssText = `
        left:${Math.random() * 100}vw;
        top:-10px;
        background:${colors[Math.floor(Math.random() * colors.length)]};
        width:${Math.random() * 8 + 4}px;
        height:${Math.random() * 8 + 4}px;
        border-radius:${Math.random() > 0.5 ? '50%' : '2px'};
        animation-duration:${Math.random() * 2 + 1.5}s;
        animation-delay:${Math.random() * 0.5}s;
      `;
      document.body.appendChild(el);
      setTimeout(() => el.remove(), 3000);
    }, i * 40);
  }
}

// ══════════════════════════════════════
// LIBRARY
// ══════════════════════════════════════

function renderLibrary() {
  const grid = document.getElementById('habit-grid');
  const filtered = currentCatFilter === 'all' ? HABITS_DB : HABITS_DB.filter(h => h.category === currentCatFilter);

  grid.innerHTML = filtered.map(h => `
    <div class="habit-tile ${state.routine.includes(h.id) ? 'in-routine' : ''}" onclick="toggleRoutine('${h.id}')">
      <div class="habit-tile-icon">${h.icon}</div>
      <div class="habit-tile-name">${h.name}</div>
      <div class="habit-tile-dur">${h.duration}s · ${h.time}</div>
      <div class="habit-added-badge">
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="20,6 9,17 4,12"/>
        </svg>
      </div>
    </div>
  `).join('');
}

function filterCategory(cat) {
  currentCatFilter = cat;
  document.querySelectorAll('.cat-tab').forEach(t => t.classList.remove('active'));
  event.target.classList.add('active');
  renderLibrary();
}

function toggleRoutine(id) {
  const idx = state.routine.indexOf(id);
  if (idx >= 0) {
    state.routine.splice(idx, 1);
    showToast('Removed from routine');
  } else {
    state.routine.push(id);
    showToast('Added to routine! ✓');
  }
  saveState();
  renderLibrary();
}

// ══════════════════════════════════════
// PROGRESS
// ══════════════════════════════════════

function renderProgress() {
  updateStreakDisplay();
  renderHeatmap();
  renderBadges();
}

function renderHeatmap() {
  const grid = document.getElementById('heatmap-grid');
  const today = new Date();
  const cells = [];

  for (let i = 27; i >= 0; i--) {
    const d = new Date(today);
    d.setDate(d.getDate() - i);
    const ds = d.toISOString().split('T')[0];
    const h = state.history.find(x => x.date === ds);
    const isToday = i === 0;
    const cls = isToday ? 'today' : (h?.count >= state.routine.length ? 'perfect' : (h?.count > 0 ? 'done' : ''));
    cells.push(`<div class="heatmap-day ${cls}" title="${ds}"></div>`);
  }

  grid.innerHTML = cells.join('');
}

function renderBadges() {
  const row = document.getElementById('badges-row');
  row.innerHTML = BADGES.map(b => {
    const earned = b.type === 'streak' ? state.streak >= b.req : state.totalDone >= b.req;
    return `
      <div class="badge ${earned ? 'earned' : 'locked'}">
        <div class="badge-icon">${b.icon}</div>
        <div class="badge-name">${b.name}</div>
      </div>
    `;
  }).join('');
}

// ══════════════════════════════════════
// UTILS
// ══════════════════════════════════════

function showToast(msg) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.style.opacity = '1';
  t.style.transform = 'translateX(-50%) translateY(0)';
  setTimeout(() => {
    t.style.opacity = '0';
    t.style.transform = 'translateX(-50%) translateY(20px)';
  }, 2000);
}

// ══════════════════════════════════════
// INIT
// ══════════════════════════════════════

checkNewDay();
renderHome();

// Service Worker registration
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => {
    const swCode = `
      const CACHE = 'micro-habits-v1';
      const ASSETS = ['/'];
      self.addEventListener('install', e => { e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS))); self.skipWaiting(); });
      self.addEventListener('fetch', e => { e.respondWith(caches.match(e.request).then(r => r || fetch(e.request))); });
    `;
    const blob = new Blob([swCode], { type: 'application/javascript' });
    const url = URL.createObjectURL(blob);
    navigator.serviceWorker.register(url).catch(() => {});
  });
}
</script>
</body>
</html>
